"use client";
import { useState } from "react";
export function AddVideo(){const [open,setOpen]=useState(false),[url,setUrl]=useState(""),[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setMessage("Checking link through the provider’s official API…");try{const response=await fetch("/api/providers/google/metadata",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({url})});const data=await response.json();setMessage(response.ok?`Found: ${data.name}. Connect Google to continue.`:data.error);}catch{setMessage("Could not reach LinkPlay. Please try again.")}}return <><button className="button" onClick={()=>setOpen(true)}>Add authorized video <span>+</span></button>{open&&<div className="modal-backdrop" role="presentation" onMouseDown={()=>setOpen(false)}><form className="modal" onSubmit={submit} onMouseDown={e=>e.stopPropagation()}><button type="button" className="close" aria-label="Close" onClick={()=>setOpen(false)}>×</button><p className="eyebrow">ADD TO YOUR LIBRARY</p><h2>Paste a supported link</h2><p>LinkPlay only accesses files through official provider authorization. It never bypasses login, DRM, or sharing settings.</p><label htmlFor="video-url">Google Drive video URL</label><input id="video-url" type="url" required placeholder="https://drive.google.com/file/d/..." value={url} onChange={e=>setUrl(e.target.value)}/><button className="button" type="submit">Validate link →</button>{message&&<output>{message}</output>}</form></div>}</>}
